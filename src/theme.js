import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#7e28c4'
    },
    socondary: {
      main: '#e31b6c'
    }
  },
  typography: {
    fontSize: 24
  }
})

export default theme
