import { createTheme } from '@mui/material/styles'

const theme = createTheme({

  palette: {
    mode: 'dark',
    primary: {
      main: '#31aaff'
    }
  },
  typography: {
    fontSize: 24
  }
})

export default theme
